#include "main.h"
#include <stdio.h>

/**
 * rev_string - function that reverses a string
 * 
 * @s: the string to be reversed
 *
 * Return: void
 *
*/

void rev_string(char *s)
{
	int str_len;

	while (str_len > 0)
	{
		s--;
		str_len;
	}
}
