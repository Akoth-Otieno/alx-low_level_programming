#inlude "main.h"

/**
 * mul - a function that multiplies two integers.
 * @a: the first integer.
 * @b: the second integer.
 * Return: Always 0 (Success).
 */

int mul(int a, int b)
{
	int prod;

	prod = a * b;

	return (prod);
}
