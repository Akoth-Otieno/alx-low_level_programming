#include "main.h"

/**
 * _isupper - a function that checks for an uppercase character.
 * @c : input parameter
 * Return: 1 if c is uppercase, else return 0.
 */

int _isupper(int c)
{
	int i;

	for ( i = 0; i <= 26; i++)
	{ 
		if ( c = 'C')
			return (1);
	}

	return (0);
}
