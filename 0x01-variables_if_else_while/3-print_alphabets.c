#include <stdio.h>

/**
 * main - Function that prints the alphabet in upper and lowercase.
 *
 * Return: Always 0 (Success)
 *
*/
int main(void)
{
char a;
for (a = 'a'; a <= 'z'; a++)
{
putchar(a);
}

char A;
for (A = 'A'; a <= 'Z'; A++)
{
putchar (A);
}
putchar ('\n')

return (0);
}
