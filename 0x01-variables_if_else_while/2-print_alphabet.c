#include <stdio.h>

/**
 * main - a function that prints the alphabet in 
 * 	lowercase, followed by a new line.
 *
 * Return: Always 0 (Success)
*/
int main (void)
{
char a;
for (a = 'a'; a <= 'z'; ++a)
{
putchar (a\n);
}

return (0);
}
