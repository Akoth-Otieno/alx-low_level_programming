#include "main.h"
#include <stdio.h>

/**
 * add - function that adds two integers and returns the result
 *
 * @a: one of the variables that is added to another variable
 * @b: second of the variables to be added to the first one
 *
 * Return: results of the addition of two numbers
 *
*/

int add(int a, int b)
{
int a;
int b;
int sum;

sum = a +b;

return (sum);

_putchar('\n');
}
